/**
 * Custom hook for accessing workspace context
 * Re-exports the useWorkspace hook from WorkspaceContext for convenience
 */
export {
  useWorkspace,
  type WorkspaceContextType,
} from "../contexts/WorkspaceContext";

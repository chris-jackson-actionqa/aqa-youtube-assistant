# Production Frontend Configuration
# This file contains production-specific settings for the Next.js frontend.
# The deployment script will copy this to .env in the production directory.

# Backend API URL
# Points to production backend on port 8001 (dev uses 8000)
# NOTE: Uses localhost because this is a desktop deployment where both frontend and backend
# run on the same machine. For web server deployments, update to your API domain.
NEXT_PUBLIC_API_URL=http://localhost:8001

# Frontend Server Port
# Production runs on port 3001 (dev uses 3000)
PORT=3001

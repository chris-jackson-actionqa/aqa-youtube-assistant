# Example Backend Configuration
# Copy to .env.development or .env.production and customize
# This file serves as a template showing all available configuration options

# Database Configuration
# SQLite database file path (relative to backend directory)
DATABASE_URL=sqlite:///./youtube_assistant.db

# Server Configuration
# Port for the backend API server (dev: 8000, prod: 8001)
PORT=8000
# Host to bind to (0.0.0.0 allows external connections)
HOST=0.0.0.0

# Development Settings
# Enable hot-reload for development (disable in production)
RELOAD=true
# Log level: debug, info, warning, error, critical
LOG_LEVEL=debug

# CORS Configuration
# Comma-separated list of allowed origins
# Development frontend runs on port 3000, production on 3001
CORS_ORIGINS=http://localhost:3000
